server {
    listen 80;
    server_name discover-inspector.vione.cloud;

    # Redirect HTTP to HTTPS once SSL is configured
    # return 301 https://$host$request_uri;

    client_max_body_size 50M;

    location /uploads/ {
        alias /data/uploads/;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location / {
        proxy_pass         http://app:5000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# Uncomment and configure after obtaining SSL certificates with certbot:
# server {
#     listen 443 ssl http2;
#     server_name discover-inspector.vione.cloud;
#     client_max_body_size 50M;
#
#     ssl_certificate     /etc/letsencrypt/live/discover-inspector.vione.cloud/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/discover-inspector.vione.cloud/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#
#     location /uploads/ {
#         alias /data/uploads/;
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location / {
#         proxy_pass         http://app:5000;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
