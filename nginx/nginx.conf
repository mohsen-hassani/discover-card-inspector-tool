# Drop this file in /etc/nginx/sites-available/discover-inspector
# then: ln -s /etc/nginx/sites-available/discover-inspector /etc/nginx/sites-enabled/
# and:  nginx -t && systemctl reload nginx

server {
    listen 80;
    server_name discover-inspector.vione.cloud;

    # After certbot: uncomment the redirect and remove the rest of this block
    # return 301 https://$host$request_uri;

    client_max_body_size 50M;

    location /uploads/ {
        alias /path/to/DiscoverCardInspectorTool/uploads/;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location / {
        proxy_pass         http://127.0.0.1:5001;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# After certbot:
# server {
#     listen 443 ssl http2;
#     server_name discover-inspector.vione.cloud;
#     client_max_body_size 50M;
#
#     ssl_certificate     /etc/letsencrypt/live/discover-inspector.vione.cloud/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/discover-inspector.vione.cloud/privkey.pem;
#
#     location /uploads/ {
#         alias /path/to/DiscoverCardInspectorTool/uploads/;
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location / {
#         proxy_pass         http://127.0.0.1:5001;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
